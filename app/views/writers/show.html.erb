

<h1>Yazar <%= @writer.name %> Hakkında</h1>

<%= @writer.bio %>
		<% if current_user.present? and current_user.admin? %>
			<%= link_to "Güncelle", edit_writer_path(@writer) %>
			<%= link_to "Sil", writer_path(@writer), method: :delete %>
		<% end %>
	</tr>
</table><br>
<h1>Bu Yazarın Kitapları</h1><br>
<% @writer.books.each do |e| %>
<%=	e.name %><br>
<% end %>
<br>
<%= link_to "Yazarlara Dön", writers_path %>
