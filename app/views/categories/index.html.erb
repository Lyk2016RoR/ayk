<h1>Tüm Kategoriler</h1><hr>
<table class="table">
	<% @categories.each do |category| %>
	<tr>
    <td><%= category.id %></td>
		<td><%= link_to category.name, category_path(category) %></td>
	</tr>
	<% end %>
</table>
<hr>
<h3><%= link_to 'Yeni Kategori', new_category_path %></h3>
